{
    "require": {
        "aws/aws-sdk-php": "^3.296",
        "ext-dom": "*",
        "ext-mysqli": "*",
        "ext-curl": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^10.5"
    },
    "scripts": {
        "test": "phpunit --configuration phpunit.xml"
    }
}
